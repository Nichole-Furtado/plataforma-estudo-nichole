# Copie para .env.local (apenas se precisar apontar para um backend remoto).
#
# DESENVOLVIMENTO LOCAL: deixe vazio/sem este arquivo — o app usa http://localhost:4000.
#
# PRODUÇÃO (Vercel): defina com a URL pública do backend no Render, ex:
# NEXT_PUBLIC_API_URL=https://nichole-backend.onrender.com
NEXT_PUBLIC_API_URL=
